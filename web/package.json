{
  "name": "deckport-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static recipe-book website for deckport, generated from the TOML recipe repo at build time.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "grids": "node scripts/fetch-grids.mjs",
    "heroes": "node scripts/fetch-heroes.mjs"
  },
  "dependencies": {
    "astro": "^6.4.6",
    "smol-toml": "^1.8.0"
  }
}
